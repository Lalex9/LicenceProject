import { PlanMyShoppingValidators } from './plan-my-shopping-validators';

describe('PlanMyShoppingValidators', () => {
  it('should create an instance', () => {
    expect(new PlanMyShoppingValidators()).toBeTruthy();
  });
});
